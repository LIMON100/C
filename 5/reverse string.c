#include<stdio.h>
#include<string.h>
int main()
{
    char s[1000],word[1000];
    int i,len,j,k;
    int t;
    scanf("%d",&t);
    for(i=0;i<=t;i++)
    {
        scanf(" %[^\n");
        len=strlen(s);
        for(j=0, k=0; j<strlen(s); j++)
        {

        }
    }
}
