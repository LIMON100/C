#include<stdio.h>
#include<iostream>
using namespace std;
int main()
{
    long long int i,t,a,y,b,n,j,x,flag;
    while(scanf("%lld",&n)==1)
    {
        if(n==0)
            break;
            flag=0;
        for(i=2; i<n; i++)
        {
            while(n!=0)
            {
                t=n%i;
            }
        }
    }
  return 0;
}
